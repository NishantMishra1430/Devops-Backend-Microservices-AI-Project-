apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quant-ai-engine.fullname" . }}-test-connection"
  labels:
    {{- include "quant-ai-engine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quant-ai-engine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
