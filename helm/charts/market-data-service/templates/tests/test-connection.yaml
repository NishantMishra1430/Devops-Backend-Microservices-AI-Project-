apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "market-data-service.fullname" . }}-test-connection"
  labels:
    {{- include "market-data-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "market-data-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
